# PostgreSQL connection string (use a pooled URL on Vercel, e.g. Neon/Supabase)
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# --- Dashboard login (website access only) ---
AUTH_EMAIL="ayazkhanloru@gmail.com"
AUTH_PASSWORD="your-dashboard-password"
AUTH_SECRET="run: openssl rand -base64 32"

# --- SMTP (sends emails — separate from login) ---
SMTP_EMAIL="ayazkhan@aizaz.studio"
SMTP_PASSWORD="gmail-app-password-for-aizaz-studio-account"
SMTP_FROM_EMAIL="ayazkhan@aizaz.studio"
SMTP_FROM_NAME="Ayaz Khan"

# Production app URL for tracking pixels
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"

# --- Supabase only: direct connection for running migrations once ---
# In Supabase: Settings → Database → Connection string → URI (port 5432, not 6543)
# DIRECT_URL="postgresql://postgres.[ref]:[password]@db.[ref].supabase.co:5432/postgres"
