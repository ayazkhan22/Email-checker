# Option A (recommended): password in a separate var — avoids URL encoding issues
DATABASE_URL="postgresql://postgres.ctsukdtinxlpzubhnzwm@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DATABASE_PASSWORD="your-supabase-database-password"

# Option B: password inside the URL (must URL-encode special chars like @ # %)
# DATABASE_URL="postgresql://postgres.ctsukdtinxlpzubhnzwm:your-password@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Session pooler for migrations (npm run db:migrate)
DIRECT_URL="postgresql://postgres.ctsukdtinxlpzubhnzwm@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres"

# --- Dashboard login (website access only) ---
AUTH_EMAIL="ayazkhanloru@gmail.com"
AUTH_PASSWORD="your-dashboard-password"
AUTH_SECRET="run: openssl rand -base64 32"

# --- SMTP (sends emails — separate from login) ---
SMTP_EMAIL="ayazkhan@aizaz.studio"
SMTP_PASSWORD="gmail-app-password-for-aizaz-studio-account"
SMTP_FROM_EMAIL="ayazkhan@aizaz.studio"
SMTP_FROM_NAME="Ayaz Khan"

# Production app URL for tracking pixels
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"
